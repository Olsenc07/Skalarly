import { Component } from '@angular/core';

@Component({
  selector: 'app-side-bar-info',
  templateUrl: './side-bar-info.component.html',
  styleUrls: ['./side-bar-info.component.scss']
})
export class SideBarInfoComponent {
  // get info from appropriate route that loaded this in
  // then display appropriate info if its your profile or other profile..
  // So use the same services as main component loaded
}
