import { GlowBorderDirective } from './glow-border.directive';

describe('GlowBorderDirective', () => {
  it('should create an instance', () => {
    const directive = new GlowBorderDirective();
    expect(directive).toBeTruthy();
  });
});
